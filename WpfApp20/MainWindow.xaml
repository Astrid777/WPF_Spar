<Window x:Class="WpfApp20.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp20"
        mc:Ignorable="d"
        Title="Отчет" Height="650" Width="1200" >



    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>

        <ListBox Name="phonesList" Grid.Column="0" >
            <TextBlock FontWeight="Bold" TextDecorations="Underline" Text="Выберите представление" />
            <ListBoxItem  Selected="Table_Selected">Таблица</ListBoxItem>
            <ListBoxItem  Selected="Chart_Selected">График</ListBoxItem>
        </ListBox>

        <Canvas x:Name="canv" Grid.Column="1" >
            <Frame x:Name="fr" Grid.Column="1" Source="Page1.xaml" 
               HorizontalAlignment="Stretch" Opacity="{Binding Opacity}"  NavigationUIVisibility="Hidden" Navigated="fr_Navigated"></Frame>
        </Canvas>
    </Grid>
</Window>
